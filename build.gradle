version = '1.6.0'
group = 'com.nokiyen.preciousshot'
archivesBaseName = 'PreciousShot'

jar {
    manifest {
        attributes([
            "Specification-Title": "preciousshot",
            "Specification-Vendor": "nokiyen",
            "Specification-Version": "1.6.0", // We are version 1 of ourselves
            "Implementation-Title": project.name,
            "Implementation-Version": "${version}",
            "Implementation-Vendor" :"nokiyen",
            "Implementation-Timestamp": new Date().format("yyyy-MM-dd'T'HH:mm:ssZ"),
            "FMLCorePlugin" : "noki.preciousshot.asm.ASMLoadingPlugin", 
            "FMLCorePluginContainsFMLMod" : true
        ])
    }
}
